import React, { PureComponent } from 'react';

export default class Note extends PureComponent {
    render() {
        return (
            <div>
                Note
            </div>
        );
    }
}
