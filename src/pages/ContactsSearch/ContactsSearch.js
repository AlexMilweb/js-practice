import React, { PureComponent } from 'react';

export default class ContactsSearch extends PureComponent {
    render() {
        return (
            <div>
                contactsSearch
            </div>
        );
    }
}
